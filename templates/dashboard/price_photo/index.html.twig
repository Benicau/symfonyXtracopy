{% extends 'baseConnect.html.twig' %}

{% block title %}Magasin{% endblock %}

{% block body %}

<div class="imprimante">
    <div class="container">
        <div class="row">
            <h1>Gestion photos de <span>Xtra-Copy</span></h1>
            <div class="menuPrint">
                <a href="{{ path('NewPhoto') }}" class="ajout">Ajouter un prix photo</a>
                <a href="{{ path('app_MenuAdmin') }}" class="ajout">Retour</a>
            </div>
        </div>
        <div class="row">
            <table>
                <thead>
                    <tr>
                        <th>Nom</th>
                        <th>Prix</th>
                        <th>Actions</th>
                    </tr>
                </thead> 
                <tbody>
                
                    <tr>
                    {% for type in TypesPhotos %}
                        <td>{{type.Name}}</td>
                        <td>{{type.Price}} €</td>
                        <td class="link"><a href="{{path('EditPhoto',{'id': type.id})}}" class="btn btn-warning mx-2">Modifier</a>
                            <a href="{{path('DeletePhoto',{'id': type.id})}}" class="btn btn-danger mx-2">Supprimer</a>
                        </td>
                    </tr>
                     {% endfor %}
                </tbody>

                
            </table>
        </div>
    </div>
</div>
{% endblock %}
