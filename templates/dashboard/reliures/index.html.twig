{% extends 'baseConnect.html.twig' %}

{% block title %}Magasin{% endblock %}

{% block body %}

<div class="imprimante">
    <div class="container">
        <div class="row">
            <h1>Gestion des type de reliures de <span>Xtra-Copy</span></h1>
            <div class="menuPrint">
                <a href="{{ path('reliureNew') }}" class="ajout">Ajouter un type de reliure</a>
                <a href="{{ path('app_MenuAdmin') }}" class="ajout">Retour</a>
            </div>
        </div>
        <div class="row">
            <table>
                <thead>
                    <tr>
                        <th>Nom</th>
                        <th>Prix</th>
                        <th>Actions</th>
                    </tr>
                </thead> 
                <tbody>
                
                    <tr>
                    {% for reliure in reliures %}
                        <td>{{reliure.Name}}</td>
                        <td>{{reliure.Price}} €</td>
                        <td class="link"><a href="{{path('reliureEdit',{'id': reliure.id})}}" class="btn btn-warning mx-2">Modifier</a>
                            <a href="{{path('reliureDelete',{'id': reliure.id})}}" class="btn btn-danger mx-2">Supprimer</a>
                        </td>
                    </tr>
                     {% endfor %}
                </tbody>

                
            </table>
        </div>
    </div>
</div>
{% endblock %}